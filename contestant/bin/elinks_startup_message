You are about to start the elinks page viewer.

It is important to remember that if you want a menu you
must type ESC.  Then `Help' is a menu item.  Under the
`Help' menu item try `Keys'.

The most common commands for this viewer are:

    Up and Down Keys: Move between links on the current page
    Left and Right Keys: Go to the page of the current link
         or return to the previous page.
    q: quit
